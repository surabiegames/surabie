# Packages Workspace

This folder is reserved for shared packages in the Surabie monorepo.

Planned packages:
- `packages/db` (Prisma schema/client wrapper)
- `packages/ui` (shared UI components)
- `packages/accounting-core` (ledger services and report calculators)
